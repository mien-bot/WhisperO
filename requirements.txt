# Runtime
sounddevice
numpy
requests
faster-whisper>=1.1.0
pynput
pyperclip
pystray
Pillow

# Optional platform clipboard support
pywin32; platform_system == "Windows"
# pyobjc (optional, for full clipboard save/restore on macOS)
# pip install whispero[macos]
# pyobjc-framework-AppKit; platform_system == "Darwin"
# pyobjc-framework-Foundation; platform_system == "Darwin"

# Speaker diarization uses ONNX at runtime (onnxruntime bundled via faster-whisper).
# Model is downloaded on demand from the app — no extra pip install needed.
# To re-export the ONNX model: pip install speechbrain torch

# Build (only needed for packaging .exe/.app)
# pip install pyinstaller
